import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  page: {
    height: '100vh',
  },
  container: {
    height: '100%',
  },
  card: {
    minWidth: 320,
    width: 480,
  },
}));
